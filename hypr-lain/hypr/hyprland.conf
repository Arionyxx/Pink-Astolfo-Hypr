# minimal, error-proof base
general {
  # keep only options that are guaranteed everywhere
  border_size = 2
}

# keep decoration empty for now to avoid version-specific opts
decoration {
  # leave blank for now
}

# basic binds so you can use the session
bind = SUPER, RETURN, exec, kitty
bind = SUPER, Q, killactive,
bind = SUPER, D, exec, rofi -show drun
